type YesOrNo = "Yes" | "No";

interface Person {
    name: string;
    age: number;
}

const names: string = "onion";

const signatureCanvas = document.getElementById("signature") as HTMLCanvasElement;

